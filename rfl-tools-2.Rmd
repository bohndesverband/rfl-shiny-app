---
title: "RFL Tools"
output: 
  flexdashboard::flex_dashboard:
    orientation: rows
    vertical_layout: scroll
    css: styles.css
    theme:
      version: 4
      bg: "#ffffff"
      fg: "#485460"
      primary: "#808e9b"
      base_font: 
        google: Poppins
      heading_font:
        google: Poppins
      code_font:
        google: 
          family: JetBrains Mono
          local: false
runtime: shiny
---

```{js}
$("body").on("shown.bs.tab", "a[data-toggle='tab']", function(e) {
  Shiny.setInputValue("active_tab", $(this).attr('href'));
   console.log($(this).attr('href'));
})

```

```{r setup, include=FALSE}
library(flexdashboard)
library(tidyverse)
library(nflreadr)
library(ffscrapr)
library(DT)
library(ggplot2)
#library(scales)
library(ggbump)
library(gt)
#library(formattable)
library(gtExtras)
library(svglite)
#library(webshot2)
library(emoji)
#library(ggridges)
library(ggrepel)
library(plotly)
library(ggalt)
library(shinyWidgets)
library(waffle)
library(shinycssloaders)
library(ggforce)
#library(shinyjs)
```

```{r global, message=TRUE, warning=TRUE, include=FALSE}
source("R new/base_data/variables.R", local = TRUE)
source("R new/base_data/league.R", local = TRUE) # als erstes
source("R new/base_data/elo.R", local = TRUE)
source("R new/base_data/player_scores.R", local = TRUE)
source("R new/base_data/fantasy_finishes.R", local = TRUE) # nach player_scores
source("R new/base_data/draft.R", local = TRUE) # nach fantasy_finishes
source("R new/base_data/rosters.R", local = TRUE)
source("R new/base_data/standings.R", local = TRUE)
source("R new/base_data/starter.R", local = TRUE)
source("R new/base_data/trades.R", local = TRUE)
source("R new/base_data/war.R", local = TRUE)
source("R new/base_data/mfl-players.R", local = TRUE) # nach ELO & rosters

options(spinner.color = color_grey_dark)
tags$head(tags$meta(charset = "UTF-8"))
```

# Sidebar {.sidebar}

```{r inputs, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/base_data/inputs.R", local = TRUE)

#textOutput("active_tab")

shiny::sliderInput(
  "selectYears",
  "Saisons",
  min = 2016,
  max = new_season_sept,
  value = c(new_season_sept, new_season_sept),
  step = 1,
  sep = ""
)

shiny::sliderInput(
  "selectYear",
  "Saison",
  min = 2016,
  max = new_season_sept,
  value = new_season_sept,
  step = 1,
  sep = ""
)

shiny::sliderInput(
  "selectDraftRounds",
  "Draft Runden",
  min = 1,
  max = 7,
  value = c(1, 7),
  step = 1,
  sep = ""
)

shinyWidgets::pickerInput(
  "selectRflTeams",
  "RFL Teams",
  choices = setNames(franchises$franchise_id[order(franchises$franchise_name)], franchises$franchise_name[order(franchises$franchise_name)]),
  #selected = franchises$franchise_id,
  #selected = "0007",
  options = list("actions-box" = TRUE, "max-options" = 6, "max-options-text" = "Es können nicht mehr Teams gewählt werden", "none-selected-text" = "Team wählen"),
  multiple = TRUE
)

shiny::selectizeInput(
  "selectRflTeam",
  "RFL Team",
  choices = setNames(franchises$franchise_id[order(franchises$franchise_name)], franchises$franchise_name[order(franchises$franchise_name)]),
  multiple = FALSE,
  #selected = "0007"
  options = list(
    placeholder = "Team wählen",
    onInitialize = I('function() { this.setValue(""); }')
  )
)

shinyWidgets::pickerInput(
  "selectRflDivisions",
  "RFL Divisions",
  choices = setNames(unique(franchises$division[order(franchises$division_name)]), unique(franchises$division_name[order(franchises$division_name)])),
  options = list("actions-box" = TRUE, "max-options" = 1, "none-selected-text" = "Division wählen"),
  multiple = TRUE,
)

shiny::selectizeInput(
  "selectRflConference",
  "RFL Conferences",
  choices = unique(franchises$conference_name[order(franchises$conference_name)]),
  selected = "BFC",
  options = list(
    placeholder = "Conference wählen"
  ),
  multiple = FALSE,
)

# TODO: genutzt in magic number - evtl löschen, dafür selectRflTeams dynamisch setzen
shinyWidgets::pickerInput(
  "selectRflConferenceTeams",
  "RFL Teams",
  choices = NULL,
  #selected = franchises$franchise_id,
  #selected = "0007",
  options = list("actions-box" = TRUE, "none-selected-text" = "Team wählen"),
  multiple = TRUE
)

mfl_teams_preselection_conf <- shiny::reactive({
  franchises %>% 
    dplyr::filter(conference_name == input$selectRflConference)
})

shiny::observe({
  req(mfl_players_preselection())
  req(nrow(mfl_players_preselection()) > 0) # Prüfen, ob Daten vorhanden sind

  shinyWidgets::updatePickerInput(
    session,
    "selectRflConferenceTeams",
    choices = mfl_teams_preselection_conf()$franchise_name[order(mfl_teams_preselection_conf()$franchise_name)],
    selected = mfl_teams_preselection_conf()$franchise_name
  )
})

shinyWidgets::pickerInput(
  "selectPosition",
  "Position",
  choices = positions,
  selected = NULL,
  options = list("actions-box" = TRUE, "none-selected-text" = "Position wählen"),
  multiple = FALSE
)

shinyWidgets::pickerInput(
  "selectPositions",
  "Positionen",
  choices = list("QB", "RB", "WR", "TE", "DT", "DE", "LB", "CB", "S"),
  selected = list("QB", "RB", "WR", "TE", "DT", "DE", "LB", "CB", "S"),
  options = list("actions-box" = TRUE, "none-selected-text" = "Position wählen"),
  multiple = TRUE
)

shinyWidgets::pickerInput(
  "selectPlayers",
  "NFL Spieler",
  choices = NULL,
  multiple = TRUE,
  options = list("actions-box" = TRUE, "live-search" = TRUE, "max-options" = 6, "max-options-text" = "Es können nicht mehr Spieler gewählt werden", "none-selected-text" = "Spieler wählen"),
)

shiny::markdown("<small>**Tipp:** Wenn du oben ein RFL Team auswählst,<br>werden dir nur Spieler aus dessen Roster angezeigt.</small>")

shiny::sliderInput(
  "selectRflGames",
  "Anzahl Spiele",
  min = 0,
  max = 13,
  value = ifelse(current_week >= 13, 8, round(current_week * 0.75)),
  step = 1,
  ticks = FALSE,
  sep = ""
)

shiny::selectizeInput(
  "selectRflMatchup",
  "RFL Team",
  choices = NULL,
  multiple = FALSE,
  options = list(
    placeholder = "Matchup wählen"
  )
)
```

> Das ELO Rating basiert auf einer Methode von [FiveThirtyEight](https://fivethirtyeight.com/methodology/how-our-nfl-predictions-work/). Mit Ligastart bekam jedes Team ein ELO Rating von 1500. Seit dem wird nach jedem Matchup die ELO eines jeden Teams angepasst. Faktoren hierbei sind die gegnerische ELO und die Punktedifferenz des Matchups. Vor jeder 1. Woche einer neuen Saison wird die ELO der vergangenen Saison um 1/3 in Richtung 1500 verschobenen. Das soll Roster Aktivitäten während der Offseason in der Berechnung berücksichtigen. In Woche 14 2016 fanden keine H2H Matchups statt, demzufolge hatte diese Woche keinen Einfluss auf das ELO Rating. [Ausführliche Erklärung gibt's hier](https://bohndesverband.de/wp-content/uploads/2024/05/rfl-elo.pdf)

> WAR beschreibt, wie viele head-to-head Matchups mehr ein Owner erwarten kann zu gewinnen, wenn er einen bestimmten Spieler aufstellt. Als Vergleichswert wird ein Replacement-Level berechnet. [Mehr dazu gibts hier zu lesen](https://www.fantasypoints.com/nfl/articles/season/2021/fantasy-war-part-1-theory)

> Das ELO Rating basiert auf einer Methode von [FiveThirtyEight](https://fivethirtyeight.com/methodology/how-our-nfl-predictions-work/). Mit Ligastart bekam jeder Spieler ein ELO Rating von 1500. Seit dem wird nach jedem Spiel die ELO eines jeden Spielers angepasst. [Ausführliche Erklärung gibt's hier](https://bohndesverband.de/wp-content/uploads/2024/05/rfl-elo.pdf)

> Die "Magic Number" ist die Summe an Siegen des führenden Teams und den Niederlagen des Verfolgerteams in den verbleibenden Spielen, die das Verfolgerteam aus dem entsprechendem Platzierungskampf eliminiert. **Faustregel:** je kleiner der Wert, desto unwahrscheinlicher ist es, für das obere Team auf das an der Seite aufzuholen.

> Home Run = mind. 2 Top 5 (1 Top 8 für junge Spieler); mind. 3 Top 5 oder 6 Top 12 Finishes für QB, TE und PK; mind. 3 Top 12 oder 6 Top 24 Finishes für alle anderen Positionen<br><br>
Hit = mind. 2 Top 8 oder 4 Top 12 für QB, TE und PK (1 Top 12 für junge Spieler); 2 Top 24 oder 4 Top 36 für alle anderen Positionen (1 Top 24 für junge Spieler)<br><br>
Miss = kein Top 24 Finish für QB, TE und PK, kein Top 48 Finish für RB, DL und DB und kein Top 60 Finish für WR und LB

# Conference Ranking {data-navmenu="Liga"}

## 

```{r conference-standing, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/rankings/standing.R", local = TRUE)
source("R new/rankings/ranking_tables.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("conf_standing_table")),
      width = 12
    )
  )
)

```

# Magic Number {data-navmenu="Liga"}

## 

```{r magic-number, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/rankings/magic_number.R", local = TRUE)

if (current_standing$week[1] < 13) {
  shiny::fluidPage(
    shiny::fluidRow(
      shiny::column(
        shinycssloaders::withSpinner(gt::gt_output("magic_number_table")),
        width = 12
        )
      )
    )
} else {
  div(
    style = "text-align: center; font-size: 18px; color: #f53b57; margin-top: 20px; padding-left: 30px;",
    "Die Saison ist vorbei, hier gibt es nichts mehr zu sehen. Schau dir stattdessen das Conference Standing an."
  )
}
```

# Power Ranking {data-navmenu="Liga"}

##

```{r power-ranking, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/rankings/power_ranking.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(shiny::plotOutput("power_ranking")),
      width = 12
    ),
    style = "height: 1030px"
  ),
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("power_ranking_table")),
      width = 12
    )
  )
)
```

# WAR {data-navmenu="Teams"}

## 

```{r team-war, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/rankings/draft_order.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("roster_war")),
      width = 12
    )
  )
)
```

# ELO Ranking {data-navmenu="Teams"}

##

```{r team-elo, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/rankings/elo.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shiny::fluidRow(
        shiny::column(
          shinycssloaders::withSpinner(shiny::plotOutput("running_elo")),
          width = 12
        ),
        style = "height: 830px"
      ),
      shiny::fluidRow(
        shiny::column(
          shinycssloaders::withSpinner(shiny::plotOutput("elo_change")),
          width = 12
        ),
        style = "height: 1230px"
      ),
      width = 8
    ),
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("elo_ranking_table")),
      width = 4
    ),
    style = "height: 830px"
  )
)
```

# Roster Tiefe {data-navmenu="Teams"}

##

```{r team-depth, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/roster/depth.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(shiny::plotOutput("roster_depth_season")),
      width = 6
    ),
    shiny::column(
      shinycssloaders::withSpinner(shiny::plotOutput("roster_depth_weekly")),
      width = 6
    )
  )
)
```

# IR Analyse {data-navmenu="Teams"}

## 

```{r ir, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/roster/ir.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shiny::plotOutput("ir_weekly") %>% 
        shinycssloaders::withSpinner(),
      width = 12
    ),
    style = "height: 830px"
  ),
  shiny::fluidRow(
    shiny::column(
      shiny::plotOutput("ir_player"),
      width = 6
    ),
    shiny::column(
      shiny::fluidRow(
        shiny::plotOutput("ir_fpts"),
        style = "height: 1000px"
      ),
      shiny::fluidRow(
        shiny::plotOutput("ir_roster"),
        style = "height: 1500px"
      ),
      width = 6
    ),
    style = "height: 2525px"
  )
)
```

# Punktehistorie {data-navmenu="Teams"}

##

```{r team-point-history, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/roster/point_history.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("roster_point_history")),
      width = 12
    )
  )
)
```

# Wins above Replacement {data-navmenu="Spieler"}

##

```{r player-war, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/players/war.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("player_war")),
      width = 8
    ),
    shiny::column(
      shinycssloaders::withSpinner(shiny::plotOutput("war_best_players")),
      width = 4
    ),
    style = "height: 1030px"
  )
)
```

# ELO {data-navmenu="Spieler"}

## 

```{r player-elo, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/players/elo.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shiny::fluidRow(
        shiny::column(
          shinycssloaders::withSpinner(shiny::plotOutput("running_player_elo")),
          width = 12
        ),
        style = "height: 680px"
      ),
      shiny::fluidRow(
        shiny::column(
          shinycssloaders::withSpinner(gt::gt_output("player_elo_peaks_table")),
          width = 12
        ),
        style = "height: 700px"
      ),
      width = 6
    ),
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("player_elo_ranking_table")),
      width = 6
    ),
    style = "height: 800px"
  )
)
```

# Fantasy Finishes {data-navmenu="Spieler"}

## 

```{r player-fantasy-finishes, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/players/fantasy_finishes_output.R", local = TRUE)

# TODO: reset selectYears to 2016
# Add grouped Positions to SelectizeInput

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("fantasy_finishes")),
      width = 12
    )
  )
)
```

# Trade Bait {data-navmenu="Spieler"}

## 

```{r trade-bait, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/players/elo.R", local = TRUE)
source("R new/players/trade_bait.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    #shiny::column(
      #shinycssloaders::withSpinner(shiny::plotOutput("running_player_elo")),
    #  width = 6
    #),
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("trade_bait")),
      width = 6
    ),
    style = "height: 680px"
  )
)
```

# Trade History {data-navmenu="Spieler"}

## 

```{r trade-history, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/players/trades.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      tags$h5("Trade History"),
      shinyWidgets::pickerInput(
        "selectTradeAsset",
        "Filter nach Asset",
        choices = "",
        options = list("actions-box" = TRUE, "none-selected-text" = "Asset wählen", "live-search" = TRUE),
        multiple = TRUE
      ),
      shinycssloaders::withSpinner(DT::DTOutput("trade_history")),
      width = 12
    )
  ),
  shiny::fluidRow(
    shiny::column(
      tags$h5("Am meisten getradete Spieler seit 2016"),
      shinycssloaders::withSpinner(DT::DTOutput("most_tradet_players")),
      width = 6
    ),
    shiny::column(
      tags$h5("Anzahl von Trades zwischen zwei Teams seit 2016"),
      shinycssloaders::withSpinner(DT::DTOutput("trades_between_teams")),
      width = 6
    )
  ),
  shiny::fluidRow(
    shiny::column(
      tags$h5("Team-Trades je Jahr"),
      shinycssloaders::withSpinner(DT::DTOutput("trades_by_franchise")),
      width = 12
    )
  )
)
```

# Awards {data-navmenu="Spieler"}

## 

```{r player-awards, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/players/awards.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(shiny::uiOutput("player_awards")),
      shinycssloaders::withSpinner(shiny::uiOutput("best_players")),
      shinycssloaders::withSpinner(shiny::uiOutput("league_mvps")),
      width = 12
    )
  )
)
```

# Projections {data-navmenu="Matchups"}

##

```{r matchup-projections, eval=FALSE, message=FALSE, warning=FALSE, include=FALSE}
source("R new/matchups/projections.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("matchup_projections_home_table")),
      width = 6
    ),
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("matchup_projections_away_table")),
      width = 6
    )
  )
)
```

# Alle Matchups {data-navmenu="Matchups"}

##

```{r matchup-history, eval=FALSE, message=FALSE, warning=FALSE, include=FALSE}
source("R new/matchups/history.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("rfl_matchup_history_table")),
      width = 12
    )
  )
)
```

# Upsets {data-navmenu="Matchups"}

##

```{r upsets, eval=FALSE, message=FALSE, warning=FALSE, include=FALSE}
source("R new/matchups/history.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("rfl_matchup_upsets_table")),
      width = 12
    )
  )
)
```

# Basics {data-navmenu="Draft"}

```{r draft-basics, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/draft/basics.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      #shinycssloaders::withSpinner(plotly::plotlyOutput("draft_basics_average")),
      shinycssloaders::withSpinner(shiny::plotOutput("rfl_picks_over_nfl_rounds")),
      width = 12
    )
  )
)
```

# Draftklassen {data-navmenu="Draft"}

```{r draft-classes, echo=FALSE, message=FALSE, warning=FALSE, out.height="900px"}
source("R new/draft/classes.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      tags$p("Klicke auf einen Datenpunkt um dir diese Draftklasse im Detail anzuschauen."),
      shinycssloaders::withSpinner(plotly::plotlyOutput("draft_classes")),
      width = 12
    )
  ),
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("single_draft_class")),
      width = 5
    ),
    shiny::column(
      tags$h4("Alle Trades mit Picks dieses Drafts"),
      shinycssloaders::withSpinner(DT::DTOutput("single_draft_class_trades")),
      width = 7
    )
  )
)
```

# Hit Rates {data-navmenu="Draft"}

##

```{r draft-hit-rates, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/draft/hit_rates.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(shiny::plotOutput("draft_hit_rates_plot")),
      width = 12
    ),
    style = "height: 900px"
  ),
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("team_hit_rates_table")),
      width = 12
    )
  ),
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(shiny::plotOutput("draft_hit_rates_by_round")),
      width = 12
    ),
    style = "height: 900px"
  ),
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("hit_rates_fantasy_finishes")),
      width = 12
    )
  )
)
```

# Draft Reihenfolge {data-navmenu="Draft"}

##

```{r draft-order, echo=FALSE, message=FALSE, warning=FALSE}
source("R new/roster/war.R", local = TRUE)

shiny::fluidPage(
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(shiny::plotOutput("draft_order")),
      width = 12
    ),
    style = "height: 1030px"
  ),
  shiny::fluidRow(
    shiny::column(
      shinycssloaders::withSpinner(gt::gt_output("draft_order_table")),
      width = 12
    ),
    style = "height: 1030px"
  )
)
```
